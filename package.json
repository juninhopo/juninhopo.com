{
  "name": "juninhopo.com",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p dist && cp -r public dist/ && cp server.js dist/ && cp package.json dist/"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^18.6.2",
    "nodemon": "^3.1.7"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
